package pojo;


import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.stereotype.Component;

@Component
public class User3 {
	@Value("${id}")
	private int id;
	@Value("${name}")
	private String name;
	@Autowired
//	@Qualifier("dog")
	
	@Value("#{@list}")
	private List list;
	@Autowired
	private Dog dog;
	@Autowired
	private Cat cat;
	@Override
	public String toString() {
		return "User3 [id=" + id + ", name=" + name + ", list=" + list
				+ ", dog=" + dog + ", cat=" + cat + "]";
	}
	
	
	
	
}
